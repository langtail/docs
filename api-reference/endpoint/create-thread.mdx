---
title: "Create a Thread"
openapi: "POST /v2/threads"
---

This endpoint creates a new thread. It allows you to initialize a conversation thread, optionally providing metadata to associate with it.
